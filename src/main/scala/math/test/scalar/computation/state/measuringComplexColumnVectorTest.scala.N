package math.test.scalar.computation.state

import scala.util.{Random}

import math.examples.scalar.computation.state.{
  measuredNormedComplexColumnVector,
  twiceMeasuredNormedComplexColumnVector
}

@main def measuringComplexColumnVectorTest() =

  given Random =
    Random({
      println("please provide a Long random generator seed")
      println("note: 123 and 654321 will give different results")
      val l = scala.io.StdIn.readLong()
      println()
      l
    })

  println()

  println(
    "measuredNormedComplexColumnVector == twiceMeasuredNormedComplexColumnVector"
  )
  println(
    "---------------------------------------------------------------------------"
  )
  println()
  print(measuredNormedComplexColumnVector)
  println("==")
  println(twiceMeasuredNormedComplexColumnVector)
